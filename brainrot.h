#include <stdio.h>
#include <stdbool.h>

# define fuck int main(int argc, char **argv) {
# define it
# define we
# define ball

# define set(x, y, z) x y = z

# define yap printf

# define serving for(
# define until ;
# define by ;
# define yk ){

# define sus if(
# define or } else if (
# define maybe 
# define otherwise } else {

# define no !
# define cap false

# define DEAD }
# define SOB 
# define BROKEN
# define WILT

# define crash return
# define out

# define alpha >
# define beta <
# define is ==

# define bump(x) x++
